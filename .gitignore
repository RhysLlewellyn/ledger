node_modules/
.next/
out/
build/
dist/

# Secrets. The connection string and API keys live here and never in the repo.
.env
.env.*
!.env.example

.vercel/
*.tsbuildinfo
next-env.d.ts

coverage/
.vitest/

Thumbs.db
desktop.ini
.DS_Store

.vercel

.vercel/

# Skills installed for the design pass, not part of the build.
.agents/
.claude/

# Sweep output. Generated by tools/a11y-sweep.mjs; not a deliverable.
sweep.json
sweep.err

# Lighthouse run output. Regenerate rather than commit.
lh/
lh-desktop.json

# Design-critique snapshots. Working notes from the design passes, kept locally
# so /impeccable polish can read the backlog; not part of the build.
.impeccable/
