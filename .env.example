# Postgres. Neon in production, a local container for the seed and the tests.
#
# The performance test reads this too. It asserts a wall-clock threshold
# against the seeded dataset, so it needs a real database with real rows in
# it -- a mock would assert that a stub returns quickly.
DATABASE_URL=postgres://ledger:ledger@localhost:5434/ledger
